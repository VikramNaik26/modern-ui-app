const Button = ({ styles }: any) => (
  <button
    className={`py-4 px-6 bg-blue-gradient font-poppins font-medium text-[18px] text-primary outline-none ${styles} rounded-xl`}
  >
    Get started
  </button>
)
export default Button
